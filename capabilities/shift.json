{
  "$schema": "https://schema.tauri.app/capability/2",
  "identifier": "shift",
  "description": "Grants access to launch command for the shift-launch window.",
  "windows": ["*"],
  "remote": {
    "urls": ["mbf://localhost/shift"]
  },
  "permissions": [
    "core:default",
    "allow-launch-with-args"
  ]
}
