{
  "$schema": "https://schema.tauri.app/capability/2",
  "identifier": "shift-launch",
  "description": "Grants access to help-text and launch commands for the shift-launch window only. These commands are intentionally excluded from the default capability so they cannot be invoked by the main MBF app or any external origin.",
  "windows": ["shift"],
  "permissions": [
    "core:default",
    "allow-get-help-text",
    "allow-launch-with-args"
  ]
}
